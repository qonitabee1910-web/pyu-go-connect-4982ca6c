/**
 * Auth Middleware stub - client-side project
 * Server-side JWT validation is handled by Supabase RLS and edge functions.
 */

export function isTokenExpired(_token: string): boolean {
  return false;
}
